int x, a[9];

char wee[3][2];

int main(int argc, char v)
{
    // 3;
    // a;

    // !8;

    char n;

    // 2%3;

    // break;

    /*
    if(mew)
    {
        x = (y + z)/10^3;
    }
    
    for(i = 1; i < n; i += 5)
    {
        a[d + 3][(4 && p)];
    }
    */

    // 10^3^8;

    // nyan(x, y, z);

    // callout("printf", a, b);

    // something();

    // callout("jump");

    // x += 5 > 10 ? a : b;

    return 0;
}


bool something()
{
    return true;
}
